You are diagnosing a classification taxonomy, not classifying for real - this is a research
tool for understanding how well a model can discriminate relevant from irrelevant taxonomy
values, not a normal classification pass.
{%- if facet_scope %}
For each comment, score its relevance against every value in the **{{ facet_scope }}** facet
only - {{ facet_description }}. You are being asked about this one facet in isolation; the
comment may also touch other facets, but those are being asked about in separate calls - only
score {{ facet_scope }}'s values here.
{%- else %}
For each comment, score its relevance against every value in every facet of the taxonomy below.
{%- endif %}

Assign a relevance score from 0.0 (no relevance at all) to 1.0 (a clear, obvious match) to
**every single value listed below** - not just the ones you'd normally tag in a real
classification pass. This is deliberately exhaustive: score the values that don't fit as low
(e.g. 0.0-0.1), but still include every one - you must return exactly one `scores` entry per
taxonomy value{% if facet_scope %} in this facet{% endif %}, so do not skip any because they
seem obviously irrelevant.

# Taxonomy
{% if facet_scope -%}
## {{ facet_scope }}: {{ facet_description }}
{% for v in facet_values %}
- {{ v.value }}: {{ v.description }}
{%- endfor %}
{% else -%}
{{ taxonomy_block }}
{%- endif %}
{% if tep_body_block %}
# TEP proposal document (context only - score the comments below, not this text)
{{ tep_body_block }}
{% endif %}

Return JSON: a `results` array with one entry per comment_id below, each entry having a
`comment_id` (integer, copied from the comment) and a `scores` array covering every taxonomy
value{% if facet_scope %} in {{ facet_scope }}{% endif %}. Each entry in `scores` must have
exactly three fields: `facet` (the facet name, copied exactly from the taxonomy), `value` (the
taxonomy value name, copied exactly), and `score` (the 0.0-1.0 relevance rating). Use these exact
field names.
