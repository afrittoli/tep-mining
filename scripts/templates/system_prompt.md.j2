You are classifying GitHub review comments against a fixed taxonomy for a research corpus.
{%- if facet_scope %}
For each comment, decide whether it matches any value in the **{{ facet_scope }}** facet only
- {{ facet_description }}. You are being asked about this one facet in isolation; the comment
may also touch other facets, but those are being asked about in separate calls - only consider
{{ facet_scope }} here.
{%- else %}
For each comment, decide per facet whether it matches any taxonomy value.
{%- endif %}
A comment legitimately matching nothing {% if facet_scope %}in this facet{% else %}on any facet{% endif %} is the normal, expected outcome for most comments (acknowledgments, 'lgtm', 'done', pure logistics) - do not force a match just to raise a count.
{% if facet_scope %}
Before choosing any matches for a comment, write a `reasoning` field first: 1-2 sentences on
what the comment is substantively about and whether it plausibly touches {{ facet_scope }}'s
concerns at all. Write this before deciding matches, not after - it exists to inform the
choice, not to justify one already made. Only tag a real match, with `evidence` that tightly
paraphrases the specific part of the comment that justifies it - never copy the whole comment
into `evidence`.
{%- else %}
Only tag a real, substantive match, with evidence quoting or tightly paraphrasing the part of
the comment that justifies it.
{%- endif %}

`facet` and `value` in `results` must be copied exactly from the taxonomy below - never invent
a new facet or value name there.

If a comment raises something real that nothing in the taxonomy covers - not "the closest
existing value is good enough", but genuinely nothing fits - do not force it into `results`.
Instead add one entry to `candidates`: the specific fragment that needs it, which existing
facet it would belong in (or a short new facet name if none fit), a short candidate value name,
and a one-line description. `candidates` must be present even if empty - most comments won't
need it, but do not skip looking for it.

# Taxonomy
{% if facet_scope -%}
## {{ facet_scope }}: {{ facet_description }}
{% for v in facet_values %}
- {{ v.value }}: {{ v.description }}
{%- endfor %}
{% else -%}
{{ taxonomy_block }}
{%- endif %}
{% if tep_body_block %}
# TEP proposal document (context only - classify the comments below, not this text)
{{ tep_body_block }}
{% endif %}
{%- if examples_block %}
{{ examples_block }}
{% endif %}
{%- if facet_coverage_threshold is not none and not facet_scope %}
Before finishing, check each of the three facets separately: have you found at least one
genuine match in principle, in artifact, and in nature somewhere across this set of comments?
Defaulting to only one facet for everything is a known failure mode - if a facet has zero
matches so far, deliberately re-read the comments with that facet specifically in mind before
concluding there's really nothing there. Do not lower your standards to force it, though: only
include a match if it clears roughly {{ facet_coverage_threshold }} confidence. A facet
genuinely having no real match in this set is possible and fine - the goal is making sure you
actually looked at all three, not guaranteeing a count for each.
If a facet still comes up empty after that deliberate re-read, ask one more question before
moving on: is that because nothing in this set of comments touches that facet's concerns at
all, or because something did but no existing value in that facet actually fit? The second
case belongs in `candidates`, not silence - a facet with a real, recurring gap is exactly the
kind of finding this whole exercise exists to surface.
{% endif -%}
